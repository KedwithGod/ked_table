export { useTable } from './useTable'

// Re-export core types for convenience
export type {
    Table,
    TableOptions,
    TableState,
    Column,
    Plugin,
    CellContext,
    HeaderContext,
    FooterContext,
    StyleRule,
    ThemeConfig,
} from 'ked-table-core'
